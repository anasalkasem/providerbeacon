import { ProviderCard } from "@/components/Marketplace";
import { PublicLayout } from "@/components/SiteChrome";
import { Button } from "@/components/ui/button";
import { providers } from "@/data/marketplace";
import { ArrowRight, BadgeCheck, Search, ShieldCheck, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";

export default function Providers(){const[query,setQuery]=useState("");const visible=useMemo(()=>providers.filter(p=>`${p.name} ${p.location} ${p.description}`.toLowerCase().includes(query.toLowerCase())),[query]);return <PublicLayout><section className="border-b border-slate-200 bg-white"><div className="container grid gap-8 py-14 lg:grid-cols-[1fr_420px] lg:items-end"><div><div className="eyebrow light"><Sparkles className="size-4"/>Provider directory</div><h1 className="mt-5 text-4xl font-extrabold tracking-tight text-slate-950 sm:text-6xl">Know who you are buying from.</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-600">Independent profiles combine verification, service history, user feedback and operational signals.</p></div><label className="relative"><span className="sr-only">Search providers</span><Search className="absolute start-4 top-1/2 size-5 -translate-y-1/2 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} className="h-14 w-full rounded-2xl border border-slate-200 bg-slate-50 ps-12 pe-4 text-sm outline-none focus:border-blue-500 focus:bg-white" placeholder="Search provider or market"/></label></div></section><section className="container py-12"><div className="flex items-center justify-between"><div><p className="section-kicker">Evaluated profiles</p><h2 className="mt-2 text-2xl font-extrabold text-slate-950">{visible.length} providers found</h2></div><div className="hidden items-center gap-2 text-sm font-semibold text-emerald-700 sm:flex"><ShieldCheck className="size-4"/>Identity-aware ranking</div></div><div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-4">{visible.map(provider=><ProviderCard key={provider.id} provider={provider}/>)}</div></section><section id="join" className="container pb-20"><div className="cta-panel"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-cyan-200">Provider onboarding</p><h2 className="mt-3 text-3xl font-extrabold text-white">Build a profile buyers can verify.</h2><p className="mt-4 max-w-xl leading-7 text-slate-300">Claim your provider profile and submit business details for an independent trust review.</p></div><Button asChild className="h-12 rounded-xl bg-white px-6 font-bold text-[#0B2A68] hover:bg-cyan-50"><a href="mailto:providers@providerbeacon.com?subject=Provider%20profile%20claim"><BadgeCheck className="size-4"/>Start verification<ArrowRight className="size-4"/></a></Button></div></section></PublicLayout>}
